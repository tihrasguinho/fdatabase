name: Publish fdatabase_web to pub.dev

on:
  push:
    tags:
      - 'v-fdatabase-web-[0-9]+.[0-9]+.[0-9]+*'

jobs:
  publishing:
    runs-on: ubuntu-latest
    steps:
      - name: 'Checkout'
        uses: actions/checkout@main
        
      - name: 'fdatabase_web'
        uses: k-paxian/dart-package-publisher@master
        with:
          credentialJson: ${{ secrets.CREDENTIAL_JSON }}
          flutter: true
          skipTests: true
          relativePath: fdatabase_web