name: Publish fdatabase_native to pub.dev

on:
  push:
    tags:
      - 'v-fdatabase-native-[0-9]+.[0-9]+.[0-9]+*'

jobs:
  publishing:
    runs-on: ubuntu-latest
    steps:
      - name: 'Checkout'
        uses: actions/checkout@main
        
      - name: 'fdatabase_native'
        uses: k-paxian/dart-package-publisher@master
        with:
          credentialJson: ${{ secrets.CREDENTIAL_JSON }}
          flutter: true
          relativePath: fdatabase_native